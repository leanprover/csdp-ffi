name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  linux:
    name: Linux (Ubuntu)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          submodules: recursive

      - name: Install BLAS / LAPACK
        run: |
          sudo apt-get update
          # `zstd` is required by our toolchain-install fallback.
          sudo apt-get install -y liblapack-dev libblas-dev gfortran zstd

      # Install elan and the toolchain pinned in lean-toolchain. We retry
      # via elan first, then fall back to a direct GitHub-release download
      # because the canonical release server is intermittently unreachable.
      - name: Install elan and Lean toolchain
        shell: bash
        run: |
          set -e
          ./scripts/install-elan.sh
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          export PATH="$HOME/.elan/bin:$PATH"
          ./scripts/install-toolchain.sh

      - name: Build
        run: lake build CSDP csdp-example

      - name: Run example
        run: |
          set +e
          lake exe csdp-example > /tmp/csdp.out 2>&1
          rc=$?
          tail -20 /tmp/csdp.out
          exit $rc

      - name: Test downstream package without native link flags
        run: ./scripts/test-downstream.sh

      - name: Test downstream editor loading
        run: python3 ./scripts/test-lsp.py

      - name: Test portable backend without writable temporary directory
        run: |
          lake clean
          real_cc="$(command -v cc)"
          probe_dir="$PWD/.lake/build/cc-probe"
          mkdir -p "$probe_dir" "$PWD/.lake/build/tmp"
          ln -s "$PWD/scripts/cc-link-temp-probe.sh" "$probe_dir/cc"
          CSDP_REAL_CC="$real_cc" \
            CSDP_EXPECTED_LINK_TMP="$PWD/.lake/build/tmp" \
            CSDP_FORCE_PORTABLE_LINALG=1 \
            PATH="$probe_dir:$PATH" \
            TMPDIR=/proc \
            lake build CSDP csdp-example
          CSDP_FORCE_PORTABLE_LINALG=1 TMPDIR=/proc lake exe csdp-example

      - name: Test native bridge with ASan and UBSan
        env:
          ASAN_OPTIONS: detect_leaks=0:strict_string_checks=1
          UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1
        run: |
          lake clean
          lake -KcsdpPortable=true -KcsdpSanitize=true build csdp-example
          lake -KcsdpPortable=true -KcsdpSanitize=true exe csdp-example

      - name: Pack and test release archive
        run: ./scripts/build-release-archive.sh release-assets

      - name: Upload release archive
        if: startsWith(github.ref, 'refs/tags/v')
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: CSDP-linux-x64
          path: release-assets/CSDP-*.tar.gz

  macos:
    name: macOS (ARM64)
    runs-on: macos-15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          submodules: recursive

      - name: Install zstd (for toolchain fallback)
        run: brew install zstd

      # The lakefile hard-codes the Command Line Tools SDK path so that the
      # build works without any IO at lake configuration time. macOS runners
      # ship with Xcode but not always with CLT at /Library/Developer/...,
      # so we ensure that path exists by symlinking the active SDK there.
      - name: Ensure CLT SDK symlink
        run: |
          sdk_path="$(xcrun --show-sdk-path)"
          target="/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk"
          echo "active sdk: $sdk_path"
          if [ ! -d "$target" ]; then
            sudo mkdir -p "$(dirname "$target")"
            sudo ln -sfn "$sdk_path" "$target"
          fi
          ls -ld "$target"

      # Install elan and the toolchain pinned in lean-toolchain. We retry
      # via elan first, then fall back to a direct GitHub-release download
      # because the canonical release server is intermittently unreachable.
      - name: Install elan and Lean toolchain
        shell: bash
        run: |
          set -e
          ./scripts/install-elan.sh
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          export PATH="$HOME/.elan/bin:$PATH"
          ./scripts/install-toolchain.sh

      - name: Build
        run: lake build CSDP csdp-example

      - name: Run example
        run: |
          set +e
          lake exe csdp-example > /tmp/csdp.out 2>&1
          rc=$?
          tail -20 /tmp/csdp.out
          exit $rc

      - name: Test downstream package without native link flags
        run: ./scripts/test-downstream.sh

      - name: Pack and test release archive
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          MACOSX_DEPLOYMENT_TARGET: '13.0'
        run: ./scripts/build-release-archive.sh release-assets

      - name: Upload release archive
        if: startsWith(github.ref, 'refs/tags/v')
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: CSDP-macos-arm64
          path: release-assets/CSDP-*.tar.gz

  windows:
    name: Windows (MSYS2 / mingw-w64)
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          submodules: recursive

      - name: Set up MSYS2
        uses: msys2/setup-msys2@66cd2cce69caa17b53920067426061ca1de3a884 # v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-openblas
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-gcc-fortran
            git
            curl
            unzip

      # On Windows the GITHUB_PATH/GITHUB_ENV files written from the MSYS2
      # shell are not always honoured for subsequent steps, so we install
      # elan, build, and run the example all in one shell session that
      # sets up PATH locally.
      - name: Install elan, build, and run example
        run: |
          set -e
          # Stage the MSYS2 BLAS / Fortran runtime libraries into a known
          # project-local path. The lakefile points `-L` at this path on
          # Windows so it works regardless of where setup-msys2 unpacks
          # (default `C:\msys64`, GitHub runners' `RUNNER_TEMP\msys64`,
          # etc.). `MINGW_PREFIX` is set by the MSYS2 shell.
          mkdir -p vendor/mingw-libs
          for pat in 'libopenblas*' 'libgfortran*' 'libquadmath*' 'libgcc*' 'libwinpthread*' 'libstdc++*'; do
            cp "$MINGW_PREFIX/lib"/$pat vendor/mingw-libs/ 2>/dev/null || true
          done
          ls vendor/mingw-libs/

          ./scripts/install-elan.sh
          ELAN_DIR_MSYS="$(cygpath -u "$USERPROFILE")/.elan"
          ls -la "$ELAN_DIR_MSYS/bin" || (echo "elan install did not produce expected files" && exit 1)
          export ELAN_HOME="$ELAN_DIR_MSYS"
          export PATH="$ELAN_DIR_MSYS/bin:$MINGW_PREFIX/bin:$PATH"
          ./scripts/install-toolchain.sh
          lake build CSDP csdp-example
          set +e
          lake exe csdp-example > /tmp/csdp.out 2>&1
          rc=$?
          tail -20 /tmp/csdp.out
          if [ "$rc" -ne 0 ]; then
            exit "$rc"
          fi
          set -e
          ./scripts/test-downstream.sh
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            ./scripts/build-release-archive.sh release-assets
          fi

      - name: Upload release archive
        if: startsWith(github.ref, 'refs/tags/v')
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: CSDP-windows-x64
          path: release-assets/CSDP-*.tar.gz

  release-additional-platforms:
    name: Release (${{ matrix.name }})
    if: startsWith(github.ref, 'refs/tags/v')
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux ARM64
            runner: ubuntu-24.04-arm
            artifact: CSDP-linux-arm64
          - name: macOS x64
            runner: macos-15-intel
            artifact: CSDP-macos-x64
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          submodules: recursive

      - name: Install zstd
        shell: bash
        run: |
          if [[ "$RUNNER_OS" == Linux ]]; then
            sudo apt-get update
            sudo apt-get install -y zstd
          else
            brew install zstd
          fi

      - name: Install elan and Lean toolchain
        shell: bash
        run: |
          set -e
          ./scripts/install-elan.sh
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          export PATH="$HOME/.elan/bin:$PATH"
          ./scripts/install-toolchain.sh

      - name: Pack and test release archive
        shell: bash
        env:
          MACOSX_DEPLOYMENT_TARGET: '13.0'
        run: ./scripts/build-release-archive.sh release-assets

      - name: Upload release archive
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ${{ matrix.artifact }}
          path: release-assets/CSDP-*.tar.gz

  release:
    name: Publish release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [linux, macos, windows, release-additional-platforms]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download platform archives
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          pattern: CSDP-*
          path: release-assets
          merge-multiple: true

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          sha256sum release-assets/CSDP-*.tar.gz > release-assets/SHA256SUMS
          release_flags=(--generate-notes)
          if [[ "$GITHUB_REF_NAME" == *-rc* ]]; then
            release_flags+=(--prerelease)
          fi
          gh release create "$GITHUB_REF_NAME" \
            release-assets/CSDP-*.tar.gz release-assets/SHA256SUMS \
            --repo "$GITHUB_REPOSITORY" \
            --verify-tag \
            --title "$GITHUB_REF_NAME" \
            --notes "The platform archives contain the Apache-2.0 and CSDP CPL-1.0 licence texts, exact source provenance, and a portable linear-algebra backend. Corresponding source is the vendored/csdp directory at this release tag." \
            "${release_flags[@]}"
